import { Ref } from 'vue';
export interface BusuanziData {
    site_pv: number;
    page_pv: number;
    site_uv: number;
}
export interface UseBuSuanZiOptions {
    /**
     * 不蒜子统计接口地址
     */
    url?: string;
    /**
     * 如果请求不蒜子接口失败，是否重试，类型 boolean
     *
     * @default false
     */
    tryRequest?: boolean;
    /**
     * 重试次数，仅当 tryRequest 为 true 时有效
     *
     * @default 5
     */
    tryCount?: number;
    /**
     * 重试间隔时间，单位毫秒，仅当 tryRequest 为 true 时有效
     *
     * @default 2000
     */
    tryIterationTime?: number;
}
/**
 * 使用不蒜子统计网站访问量
 *
 * @param immediate 是否初始化请求，即自动执行一次 request
 * @param options 配置项
 */
export declare const useBuSuanZi: (immediate?: boolean, options?: UseBuSuanZiOptions) => {
    sitePv: Ref<number, number>;
    siteUv: Ref<number, number>;
    pagePv: Ref<number, number>;
    isGet: Ref<boolean | null, boolean | null>;
    request: () => void;
};
export type UseBuSuanZiReturn = ReturnType<typeof useBuSuanZi>;
